/*
var fakedom = require("testHelpers/fakedom");
var React = require("react");
var ReactAddons = require("react/addons").addons;
var TestUtils = ReactAddons.TestUtils;
var assert = require("chai").assert;

describe.skip("HelloPage", function() {
    before(function() {
        var HelloPage = require("compiled/pages/hello");
        this.renderedComponent = TestUtils.renderIntoDocument(HelloPage);

    });

    it.("should render", function() {
        assert.isEqual("a", "a");
    });
});*/
